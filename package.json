{
  "name": "tavily-mcp",
  "version": "1.0.0",
  "description": "Tavily 搜索 MCP 服务器",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "esbuild index.js --bundle --platform=node --format=esm --outfile=dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "axios": "^1.16.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
